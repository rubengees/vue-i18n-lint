{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://raw.githubusercontent.com/rubengees/vue-i18n-lint/main/config.schema.json",
  "title": "vue-i18n-lint configuration",
  "description": "Configuration schema for vue-i18n-lint",
  "type": "object",
  "properties": {
    "format": {
      "default": "text",
      "description": "Output format for lint results",
      "type": "string",
      "enum": ["text", "json", "toon"]
    },
    "localePattern": {
      "default": "**/locales/*.json",
      "description": "Glob pattern for i18n locale files",
      "type": "string",
      "minLength": 1
    },
    "srcPattern": {
      "default": "**/*.{ts,cts,mts,js,cjs,mjs,vue}",
      "description": "Glob pattern for source files",
      "type": "string",
      "minLength": 1
    },
    "ignorePatterns": {
      "default": [],
      "description": "Glob patterns to ignore",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "ignoreKeys": {
      "default": [],
      "description": "Keys to ignore in all checks",
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "checks": {
      "description": "Severity and per-check ignore configuration",
      "type": "object",
      "properties": {
        "missingKeys": {
          "description": "Severity and ignore list for missing keys",
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "severity": {
                  "default": "error",
                  "description": "Severity level for this check",
                  "type": "string",
                  "enum": ["error", "warning", "off"]
                },
                "ignore": {
                  "default": [],
                  "description": "Keys to ignore in this check",
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1
                  }
                }
              }
            },
            {
              "type": "boolean",
              "const": false
            }
          ]
        },
        "unusedKeys": {
          "description": "Severity and ignore list for unused keys",
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "severity": {
                  "default": "warning",
                  "description": "Severity level for this check",
                  "type": "string",
                  "enum": ["error", "warning", "off"]
                },
                "ignore": {
                  "default": [],
                  "description": "Keys to ignore in this check",
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1
                  }
                }
              }
            },
            {
              "type": "boolean",
              "const": false
            }
          ]
        }
      }
    }
  }
}
